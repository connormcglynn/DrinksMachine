using System;
namespace MoshCsharp.Math
{
    public class Calculator
    {
        public class Add
        {
            int[] numbers = { 1, 2, 3 };
            // numbers[0] = 1;
            // numbers[1] = 2;
            // numbers[2] = 3;

            Console.WriteLine(numbers[1]);
        }
    }
}
